FROM nginx:alpine

# Copy static site content and Nginx config
COPY . /usr/share/nginx/html/
COPY nginx.conf /etc/nginx/nginx.conf

# Expose HTTP
EXPOSE 80

# Default command provided by base image starts nginx


